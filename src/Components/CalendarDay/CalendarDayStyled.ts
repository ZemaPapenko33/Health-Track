import { Box, styled } from '@mui/material'

export const CalendarDayWrapper = styled(Box)({
  marginRight: '0.5rem',
  display: 'flex',
  width: '14%',
  height: '12%',
  justifyContent: 'center',
  alignItems: 'center',
  border: '0.5px solid black',
  borderRadius: '0.75rem',
  cursor: 'pointer'
})
